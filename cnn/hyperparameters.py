learning_rate = 10 ** -3        # might prevent convergence
batch_size = 10
steps = 10 ** 6
dropout_rate = 0.6              # might cause underfitting
                                # could prevent overfitting
